node_modules
dist
.wrangler
.vinext
.next
next-env.d.ts
.env*
!.env.example
.dev.vars*
!.dev.vars.example
*.tsbuildinfo

.tmp-*

# Duplicate skill copies the skills CLI writes for other agents (.claude/skills is the tracked copy)
.agents/
