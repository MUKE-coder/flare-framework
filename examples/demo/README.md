# demo

A [Flare](https://github.com/) app running on Cloudflare Workers via vinext.

```bash
pnpm run dev      # start the dev server
pnpm run build    # production build
pnpm run deploy   # build and deploy to Cloudflare Workers
```
