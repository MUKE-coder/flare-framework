# __APP_NAME__

A [Flare](https://github.com/) app running on Cloudflare Workers via vinext.

```bash
__PM__ run dev      # start the dev server
__PM__ run build    # production build
__PM__ run deploy   # build and deploy to Cloudflare Workers
```
